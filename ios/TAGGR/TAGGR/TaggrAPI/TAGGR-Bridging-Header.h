// TAGGR iOS / C bridge for vetted cryptographic primitives used by the IC client.
// Swift imports only the small wrapper surface instead of depending on blst headers directly.

#include "TaggrBLSVerifier.h"
