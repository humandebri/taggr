# 安全対策の復旧修正 — 2026-09-09

## 状態

ローカル実装・対象テスト・画面検証を完了。本番復旧・公開・実機再インストールは未実施。本番権限と設定が未解決であり、リリース承認可能な状態とはまだ判断しない。

## 確定した原因と変更

規約同意の保存は正常だった。現行Workerに安全対策APIがなく404を返す一方、iOSは制限情報の取得成功を必要としていた。Retryはエラーを握りつぶしていた。

- 同意済みならタブを表示する直近修正を維持。UGC制限は維持。
- タブの読み込みタスクが制限解除を監視し、初期revisionが0でも復旧後に現在のルートをロードする。制限中には同タスクからロードしない。
- HTTPステータス、通信エラー、不正なポリシーを表示。Retryは本人確認を含む処理全体で二重実行を防ぎ、通信中を表示する。
- 画面には同意保存済みであることを明記。利用停止時はAccount内の問い合わせ場所を案内する。Contactリンクは案内画面から外し、設定に維持。
- DB未設定時に有効なポリシーや受付成功を返さないWorker回帰テストを追加。
- 既存の安全対策実装、フレーズログイン、その他の作業ツリー変更は保持。

## 検証結果

- Worker: 10/10成功。通報保存、重複送信、レート制限、JWT認証、制限・解除、通知失敗と再送、保持期間、DB未設定を含む。worker-tests.txt参照。
- Wrangler deploy --dry-run: 成功。公開や本番設定の妥当性・通知到達を保証する検査ではない。
- ローカルD1マイグレーション: 成功。artifacts/safety-recovery/local-dbのみ。
- iOS: 最終9/9成功。404→復旧、期限切れ・過長ポリシー拒否、同意保持、保存・復元、ブロック、NSFW・利用停止、制限対象投稿の拒否、実際のRootViewをホストした画面遷移なしのフィード復旧。ios-final-summary.json参照。
- idb: iPhone 17 Simulator 29047A70-52E0-4A50-971A-24E4F94C8C1E。404表示、Retryの操作、Go to AccountからSign in表示を確認。native-before.json / native-account.json参照。成功時の復帰は上記ホストUIテストで確認。
- Playwright CLI: ローカル /safety の問い合わせ受付と受付番号、未認証管理画面拒否、ローカル署名JWTでの管理画面表示、テスト投稿42の制限と解除、監査記録を確認。実メールや本番制限は発生させていない。検証セッション・サーバーは終了。
- cargo check、変更対象Prettier、git diff --check: 成功。全体make formatは無関係な既存変更や保護対象も書き換えるため実行せず、対象ファイルを整形した。
- フルiOSスイートは今回再実行していない。以前に確認した対象外の既存失敗を修正済みとは扱わない。実機UI、本番結合、通知実受信は未検証。

## 差分レビュー

実装者によるレビュー。独立した別レビュアーによるレビューではない。review-sha256.jsonに対象のハッシュを記録。

確認対象: 同意とポリシーの分離、制限中の投稿拒否、ポリシー期限・canister・バージョン検証、復旧再読み込み、Retryの取消し・失敗表示、管理JWTとOrigin、匿名通報による自動制限がないこと、DBトランザクションと監査、通報内容を通知本文へ載せないこと。

修正した指摘: ポリシー復旧だけではroute/revisionが変わらず読み込みが再開しない可能性、無効ポリシーの黙示的な拒否、本人確認中のRetry多重起動、UI回帰テストによる既定フィード設定の書換え。

未解決の公開阻害事項: 下記本番認証・リソース・メール設定、および本番結合検証。ローカルの成功だけで審査対応完了とはしない。

## 本番設定の現状と予定差分

対象: Cloudflare account 9029b5f9de5b2e820eaf4ed562bcb0e7、Worker taggr-oauth-site、taggr.kasane.network。

- 現行の最新配信バージョンは4928253c-f1eb-4f79-85f6-a6dc4358a173（2026-09-07）。バインディングと秘密値一覧は空。
- D1 taggr-ios-safetyは未作成。承認後、専用DBを作成し実IDをバインド、既存0001スキーマを適用する。他のDBを使わない。
- Access organization/apps、DNS一覧は現行Wrangler認証でHTTP403。Access設定・正確なDNS差分は未確定。権限のある管理用認証で再確認する必要がある。
- 予定Access対象は/admin/safetyと/api/admin/safety/*。既存の指定管理者だけ許可、セッション1時間。実際のissuer/audienceを本番変数へ設定する。
- SAFETY_RATE_SALTは未設定。承認後に乱数生成し秘密値へ保存する。ソース・ログに記録しない。
- kasane.networkのEmail Routingはenabled=false/status=unconfigured。Email Sending一覧もUnauthorized（2036）。送信ドメイン設定要求・DNS・利用可能プランを確認できていない。
- 予定通知は既存管理者宛てのみ、送信元safety@taggr.kasane.network、5分cron。具体的なDNSと追加費用が判明するまで契約・DNS変更・実メール送信をしない。
- 追加のライブラリ/API公開は行わない。iOS検証は既存のローカルICNativeClient workspaceを使用し、配布設定に絶対パスを入れない。

## 次の順序

1. Access・DNS・Email Sendingを確認できる管理用認証を整える（認証情報はチャットに貼らない）。
2. 不足リソース、正確なDNS差分、費用、更新済み確定差分を提示し公開承認を得る。
3. 承認対象の本番設定とWorkerのみ反映し、ポリシー・問い合わせ・管理者認証・通知実受信・テスト対象の制限と解除を確認する。
4. 本番稼働確認後に実機へ修正版を反映し、全タブと安全対策の操作を確認する。

ロールバックは記録した旧Worker版への復帰。D1の通報・監査記録は削除しない。旧版は安全対策APIを持たずiOSの投稿利用が再び止まるため、その影響を承認判断に含める。空の制限情報による回避はしない。
